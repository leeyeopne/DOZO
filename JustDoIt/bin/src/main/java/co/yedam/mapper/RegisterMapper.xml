<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yedam.mapper.RegisterMapper">

	<insert id="registerMember" parameterType="co.yedam.vo.MemberVO">
        INSERT INTO member (member_no, member_id, member_pw, member_nm, phone, email, address)
        VALUES (board_seq.nextVal, #{memberId}, #{memberPw}, #{memberNm}, #{phone}, #{email}, #{address1}||#{address2}||#{address3})
	</insert>
	
	<select id="idCheck" resultType="co.yedam.vo.MemberVO">
		select *
		from member
		where member_id = #{loginId}
	</select>
	
</mapper>